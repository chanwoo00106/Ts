"use strict";
function someFunc(val, isNumber) {
    if (isNumber) {
        val.toFixed(2);
        isNaN(val);
    }
    else {
        val.split('');
        val.toUpperCase();
        val.length;
    }
}
